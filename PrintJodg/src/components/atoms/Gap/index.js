import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Gap = ({height, widht}) => {
    return <View style={{height: height, width: widht}}/>;  
}

export default Gap;

